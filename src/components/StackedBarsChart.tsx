
type Props = {
    zone: string,
    product: string
    labels: string[]
    data: any
    }

const StackedBarsChart: React.FC<Props> = ({
    zone,
    product,
    labels,
    data
    }) => {

    return (<div>HOLA MUDNO</div>)
        }

export default StackedBarsChart;
